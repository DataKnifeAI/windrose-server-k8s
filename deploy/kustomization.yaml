apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
images:
  - name: game-server
    newName: harbor.dataknife.net/library/windrose-server-k8s
    # Immutable CI tag (same digest as latest at 4c7ab40); bump when GitLab publishes a new short SHA.
    newTag: 4c7ab40b
