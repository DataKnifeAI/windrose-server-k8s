apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyProxy
metadata:
  name: windrose-kubevip
  labels:
    app.kubernetes.io/name: windrose-server
    app.kubernetes.io/component: envoy-gateway
spec:
  provider:
    type: Kubernetes
    kubernetes:
      envoyService:
        type: LoadBalancer
        loadBalancerIP: 192.168.14.186
        externalTrafficPolicy: Cluster
