apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
images:
  - name: game-server
    newName: harbor.dataknife.net/library/windrose-server-k8s
    # Immutable GitLab CI short SHA in Harbor (bump after each release build).
    newTag: 4c7ab40b
